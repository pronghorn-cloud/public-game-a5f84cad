# Car racing game

I want to create a fun car racing game I can play online with friends


## 🚀 1-Click Deployment

This project is configured for easy deployment on popular static hosting providers.

### Render.com
Connect your repository to Render and it will automatically detect the `render.yaml` configuration.
- **Build Command:** `npm install && npm run build`
- **Publish Directory:** `dist`

### Netlify
The included `netlify.toml` handles configuration automatically.

### Vercel
The included `vercel.json` handles configuration automatically.

## Local Development

1. `npm install`
2. `npm run dev`